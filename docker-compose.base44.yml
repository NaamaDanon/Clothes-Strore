services:
  app:
    image: node:22-slim
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    command: >
      sh -c "npm install && node server/app.js"
    environment:
      - NODE_ENV=development
